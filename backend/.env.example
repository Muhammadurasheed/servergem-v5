# GitHub Personal Access Token (Required for GitHub integration)
# Get from: https://github.com/settings/tokens
# Required scopes: repo, read:user
GITHUB_TOKEN=your_github_token_here

# Google Cloud Project (Required for Cloud Run deployment and Vertex AI)
# Your GCP project ID
GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Google Cloud Region (Optional - defaults to us-central1)
GOOGLE_CLOUD_REGION=us-central1

# Server Configuration (Optional)
PORT=8000

# CORS Configuration (Optional - defaults to *)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
